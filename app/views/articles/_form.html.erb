<!--
Note:
  form_with is a new way to create forms in Rails 5.1
  il fait normalement un appel AJAX pour soumettre le formulaire avec la methode POST.
  mais on peut le forcer a faire un appel HTML en ajoutant local: true

  il faut ajouter model: @article pour que le formulaire soit pre-rempli avec les valeurs de l'article a editer

  @article est cette variable qui contient l'article a editer qui a ete recupere au niveau de la methode edit du controller
-->

<%= form_with(model: @article, local: true) do |f| %>  
  <p>
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </p>
  <p>
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>